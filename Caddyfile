{$SITE_DOMAIN} {
	encode gzip

	header {
		X-Content-Type-Options nosniff
		Referrer-Policy same-origin
		X-Frame-Options SAMEORIGIN
		Strict-Transport-Security "max-age=31536000"
	}

	reverse_proxy app:8080 {
		# SSE live feed must not be buffered
		flush_interval -1
	}
}
